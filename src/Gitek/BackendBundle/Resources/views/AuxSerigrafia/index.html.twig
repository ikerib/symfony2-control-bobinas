{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AuxSerigrafia list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Texto</th>
                <th>Completado</th>
                <th>Fecha</th>
                <th>Descripcion</th>
                <th>Mostrar</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_auxserigrafia_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.texto }}</td>
                <td>{{ entity.completado }}</td>
                <td>{% if entity.fecha %}{{ entity.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.descripcion }}</td>
                <td>{{ entity.mostrar }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_auxserigrafia_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_auxserigrafia_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_auxserigrafia_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
