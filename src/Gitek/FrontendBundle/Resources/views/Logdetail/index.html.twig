{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Logdetail list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Componente</th>
                <th>Posicion1</th>
                <th>Posicion2</th>
                <th>Cantidad</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_logdetail_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.componente }}</td>
                <td>{{ entity.posicion1 }}</td>
                <td>{{ entity.posicion2 }}</td>
                <td>{{ entity.cantidad }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_logdetail_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_logdetail_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_logdetail_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
