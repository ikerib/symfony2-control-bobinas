{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Logdetail</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Componente</th>
                <td>{{ entity.componente }}</td>
            </tr>
            <tr>
                <th>Posicion1</th>
                <td>{{ entity.posicion1 }}</td>
            </tr>
            <tr>
                <th>Posicion2</th>
                <td>{{ entity.posicion2 }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ entity.cantidad }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_logdetail') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_logdetail_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
